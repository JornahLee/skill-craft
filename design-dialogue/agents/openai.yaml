interface:
  display_name: "design-dialogue"
  short_description: "单次加载并持续控制设计讨论的焦点、节奏与深度"
  default_prompt: "使用 $design-dialogue 贯穿当前设计任务；加载一次后持续沿用，只展开当前焦点。"

policy:
  allow_implicit_invocation: true
