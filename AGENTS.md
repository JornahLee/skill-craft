用户偏好中文。

创建或修改 skill 时：
- SKILL.md 的 name 必须使用简短、含义明确的英文。
- skill 目录名必须与 SKILL.md 的 name 完全一致。
- agents/openai.yaml 的 interface.display_name 必须与 SKILL.md 的 name 完全一致，保持简短并方便主动调用。
- description、short_description、default_prompt 和正文使用中文。
